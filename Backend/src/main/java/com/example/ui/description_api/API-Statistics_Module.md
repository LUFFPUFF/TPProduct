# 📊 Statistics Module API Overview

Все эндпоинты для получения агрегированной статистики располагаются под общим префиксом:
**`/api/statistics`**

---

## 📈 Получение сводной статистики по всем модулям

**GET `/api/statistics/summary`**

Возвращает сводную информацию по метрикам различных модулей системы (аутентификация, чаты, регистрация, роли, поиск ответов, обработка текста).

**Query-параметры:**

*   `timeRange` (String, опционально, по умолчанию `1h`): Временной диапазон для агрегации статистики.
    *   Примеры: `30m` (30 минут), `1h` (1 час), `6h` (6 часов), `1d` (1 день), `7d` (7 дней).
    *   Используется, если `startTimestamp` и `endTimestamp` не предоставлены.
*   `companyId` (String, опционально): ID компании для фильтрации статистики (в основном для модуля чатов и поиска ответов). Если не указан или равен "all", статистика агрегируется по всем компаниям, где это применимо.
*   `startTimestamp` (Long, опционально): Начальная временная метка в формате Unix timestamp (секунды с начала эпохи). Если указана вместе с `endTimestamp`, то `timeRange` игнорируется.
*   `endTimestamp` (Long, опционально): Конечная временная метка в формате Unix timestamp (секунды с начала эпохи). Если указана вместе с `startTimestamp`, то `timeRange` игнорируется. Если указан только `startTimestamp`, то `endTimestamp` по умолчанию устанавливается на текущее время.
*   `step` (String, опционально): Шаг/разрешение для запросов временных рядов, если какой-либо из агрегированных DTO внутри использует данные временных рядов для своих вычислений (например, `1m`, `5m`, `1h`). В текущей реализации сводок этот параметр в основном не используется напрямую для формирования DTO, но может быть передан.

**Пример запроса:**
`GET /api/statistics/summary?timeRange=6h&companyId=some-company-uuid`

**Пример запроса с временными метками:**
`GET /api/statistics/summary?startTimestamp=1672531200&endTimestamp=1672617600&companyId=2`

---

**Response Body (JSON): `MetricSummaryDto`**

Объект, содержащий сводную статистику по различным модулям.

```json
{
  "auth": {
    // AuthSummaryStatsDTO
  },
  "chat": {
    // ChatSummaryStatsDTO
  },
  "registration": {
    // RegistrationSummaryStatsDTO
  },
  "role": {
    // RoleSummaryStatsDTO
  },
  "answerSearch": {
    // AnswerSearchSummaryStatsDTO
  },
  "textProcessing": {
    // TextProcessingSummaryStatsDTO
  }
}
```

### 📄 `MetricSummaryDto`

Корневой объект ответа, агрегирующий статистику по всем модулям.

| Поле             | Тип                             | Описание                                         |
|------------------|---------------------------------|--------------------------------------------------|
| `auth`           | `AuthSummaryStatsDTO`           | Статистика модуля аутентификации.                |
| `chat`           | `ChatSummaryStatsDTO`           | Статистика модуля чатов.                         |
| `registration`   | `RegistrationSummaryStatsDTO`   | Статистика модуля регистрации.                   |
| `role`           | `RoleSummaryStatsDTO`           | Статистика модуля управления ролями.             |
| `answerSearch`   | `AnswerSearchSummaryStatsDTO`   | Статистика модуля поиска ответов (автоответчик). |
| `textProcessing` | `TextProcessingSummaryStatsDTO` | Статистика модуля обработки текста (ML).         |


### `AuthSummaryStatsDTO`

Статистика по операциям аутентификации пользователей

| Поле                             | Тип      | Описание                                                                   |
|----------------------------------|----------|----------------------------------------------------------------------------|
| `timeRange`                      | `String` | Временной диапазон, за который собрана статистика (например, "1h", "7d").. |
| `loginSuccessCount`              | `long`   | Количество успешных попыток входа.                                         |
| `loginFailureUserNotFoundCount`  | `long`   | Количество неудачных попыток входа из-за не найденного пользователя.       |
| `loginFailureWrongPasswordCount` | `long`   | Количество неудачных попыток входа из-за неверного пароля.                 |
| `logoutSuccessCount`             | `long`   | Количество успешных выходов из системы.                                    |
| `totalAuthErrors`                | `long`   | Общее количество других ошибок, произошедших в модуле аутентификации.      |


### `ChatSummaryStatsDTO`

Статистика по операциям с чатами.

| Поле                                      | Тип               | Описание                                                                                                                                                                                      |
|:------------------------------------------|:------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `companyId`                               | String            | ID компании, для которой собрана статистика (или "all" / `null`).                                                                                                                             |
| `timeRange`                               | String            | Временной диапазон, за который собрана статистика (например, "1h", "7d").                                                                                                                     |
| `totalChatsCreated`                       | Long              | Общее количество созданных чатов за указанный `timeRange`.                                                                                                                                    |
| `totalChatsClosed`                        | Long              | Общее количество закрытых чатов за указанный `timeRange`.                                                                                                                                     |
| `totalMessagesSent`                       | Long              | Общее количество отправленных сообщений (клиентами, операторами, системой) за указанный `timeRange`.                                                                                          |
| `averageChatDurationSeconds`              | Double            | Средняя продолжительность закрытых чатов в секундах (от создания до закрытия). Может быть `null`.                                                                                             |
| `averageAssignmentTimeSeconds`            | Double            | Среднее время назначения чата оператору в секундах (от создания чата до назначения). Может быть `null`.                                                                                       |
| `averageFirstOperatorResponseTimeSeconds` | Double            | Среднее время первого ответа оператора в секундах (от назначения чата до первого сообщения оператора). Может быть `null`.                                                                     |
| `currentChatsByStatus`                    | Map<String, Long> | Текущее (на момент запроса) распределение активных чатов по статусам. Ключ - строковое представление статуса (например, "ASSIGNED", "IN_PROGRESS"), значение - количество. Может быть `null`. |
| `createdChatsByChannel`                   | Map<String, Long> | Распределение созданных чатов по каналам за указанный `timeRange`. Ключ - строковое представление канала (например, "Telegram", "Email"), значение - количество. Может быть `null`.           |


###  `RegistrationSummaryStatsDTO`

Статистика по процессу регистрации новых пользователей.

| Поле                            | Тип    | Описание                                                                                                             |
|:--------------------------------|:-------|:---------------------------------------------------------------------------------------------------------------------|
| `timeRange`                     | String | Временной диапазон, за который собрана статистика (например, "1h", "7d").                                            |
| `registrationAttempts`          | long   | Общее количество инициированных попыток регистрации за указанный `timeRange`.                                        |
| `codesSentSuccess`              | long   | Количество успешно отправленных кодов подтверждения (например, на email или SMS) за указанный `timeRange`.           |
| `codesSentFailure`              | long   | Количество неудачных попыток отправки кодов подтверждения за указанный `timeRange`.                                  |
| `codesCheckedSuccess`           | long   | Количество успешно проверенных (валидных) кодов подтверждения, введенных пользователем, за указанный `timeRange`.    |
| `codesCheckedFailureInvalid`    | long   | Количество неудачных проверок кодов из-за ввода пользователем неверного или истекшего кода за указанный `timeRange`. |
| `emailExistsDuringRegistration` | long   | Количество попыток регистрации с email, который уже существует в системе, за указанный `timeRange`.                  |
| `totalRegistrationErrors`       | long   | Общее количество других технических или бизнес-ошибок, произошедших в модуле регистрации за указанный `timeRange`.   |

---

###  `RoleSummaryStatsDTO`

Статистика по операциям назначения и снятия ролей пользователям.

| Поле                       | Тип               | Описание                                                                                                                                                                       |
|:---------------------------|:------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `timeRange`                | String            | Временной диапазон, за который собрана статистика (например, "1h", "7d").                                                                                                      |
| `rolesAddedCount`          | Map<String, Long> | Количество назначений для каждой роли за указанный `timeRange`. Ключ - строковое представление имени роли (например, "OPERATOR", "MANAGER"), значение - количество назначений. |
| `rolesRemovedCount`        | Map<String, Long> | Количество снятий для каждой роли за указанный `timeRange`. Ключ - строковое представление имени роли, значение - количество снятий.                                           |
| `userNotFoundFailures`     | long              | Количество неудачных операций с ролями из-за того, что указанный пользователь не был найден в системе, за указанный `timeRange`.                                               |
| `totalRoleOperationErrors` | long              | Общее количество других технических или бизнес-ошибок, произошедших в модуле управления ролями за указанный `timeRange`.                                                       |

### `AnswerSearchSummaryStatsDTO`

Статистика по работе модуля поиска предопределенных ответов (часто используется автоответчиком).

| Поле                          | Тип    | Описание                                                                                                                                                       |
|:------------------------------|:-------|:---------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `timeRange`                   | String | Временной диапазон, за который собрана статистика (например, "1h", "7d").                                                                                      |
| `companyId`                   | String | ID компании, для которой производился поиск ответов (или "all" / `null`, если применимо).                                                                      |
| `category`                    | String | Категория или тема, в рамках которой производился поиск ответов (или "all" / `null`, если применимо).                                                          |
| `totalRequests`               | Long   | Общее количество запросов к сервису поиска ответов за указанный `timeRange`.                                                                                   |
| `emptyQueryRequests`          | Long   | Количество запросов на поиск, где текст запроса клиента отсутствовал или был пустым, за указанный `timeRange`.                                                 |
| `longQueryRequests`           | Long   | Количество запросов на поиск, где текст запроса клиента превышал установленный лимит длины, за указанный `timeRange`.                                          |
| `totalResultsReturned`        | Long   | Суммарное количество кандидатов в ответы (например, `AnswerSearchResultItem`), возвращенных всеми успешными поисковыми запросами за указанный `timeRange`.     |
| `searchesWithNoResults`       | Long   | Количество успешно выполненных поисковых запросов, которые не вернули ни одного подходящего кандидата в ответы, за указанный `timeRange`.                      |
| `totalErrors`                 | Long   | Общее количество ошибок (например, `AnswerSearchException`, непредвиденные ошибки), возникших во время работы модуля поиска ответов, за указанный `timeRange`. |
| `averageExecutionTimeSeconds` | Double | Среднее время выполнения одного запроса на поиск ответов в секундах. Может быть `null`, если не было успешных запросов.                                        |

---

### `TextProcessingSummaryStatsDTO`

Статистика по работе модуля обработки текста, который может включать взаимодействие с ML моделями для задач вроде коррекции текста или генерации общих ответов.

| Поле                         | Тип    | Описание                                                                                                                                                       |
|:-----------------------------|:-------|:---------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `timeRange`                  | String | Временной диапазон, за который собрана статистика (например, "1h", "7d").                                                                                      |
| `totalProcessQueryRequests`  | Long   | Общее количество вызовов к методу обработки/коррекции текста (например, `processQuery`) за указанный `timeRange`.                                              |
| `totalGeneralAnswerRequests` | Long   | Общее количество вызовов к методу генерации общего ответа (например, `generateGeneralAnswer`) за указанный `timeRange`.                                        |
| `avgProcessQueryDurationMs`  | Double | Среднее время выполнения метода обработки/коррекции текста в миллисекундах. Может быть `null` или `0.0`, если не было вызовов.                                 |
| `avgGeneralAnswerDurationMs` | Double | Среднее время выполнения метода генерации общего ответа в миллисекундах. Может быть `null` или `0.0`, если не было вызовов.                                    |
| `avgApiCallDurationMs`       | Double | Среднее время одного вызова к внешнему API обработки текста (ML модели) в миллисекундах. Может быть `null`, если не было вызовов.                              |
| `totalApiCalls`              | Long   | Общее количество фактических вызовов к внешнему API обработки текста за указанный `timeRange`.                                                                 |
| `totalApiRetries`            | Long   | Общее количество повторных попыток вызова внешнего API обработки текста из-за ошибок за указанный `timeRange`.                                                 |
| `totalEmptyApiResponses`     | Long   | Количество случаев, когда внешнее API обработки текста вернуло пустой или отсутствующий результат (например, пустой `generatedText`) за указанный `timeRange`. |
| `totalValidationFailures`    | Long   | Общее количество ошибок валидации данных перед отправкой запроса к внешнему API обработки текста за указанный `timeRange`.                                     |
| `totalErrors`                | Long   | Общее количество других ошибок (например, `MLException`, непредвиденные исключения), возникших в модуле обработки текста, за указанный `timeRange`.            |